import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand ──────────────────────────────────────────────
  static const primary = Color(0xFF00C853);       // Brand green
  static const primaryDark = Color(0xFF009624);
  static const primaryLight = Color(0xFF69F0AE);
  static const accent = Color(0xFF00E676);

  // ── Light Mode ─────────────────────────────────────────
  static const lightBackground = Color(0xFFF7F8FA);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurfaceVariant = Color(0xFFF0F2F5);
  static const lightBorder = Color(0xFFE0E4EA);
  static const lightDivider = Color(0xFFEEF0F3);

  static const lightTextPrimary = Color(0xFF111827);
  static const lightTextSecondary = Color(0xFF6B7280);
  static const lightTextTertiary = Color(0xFF9CA3AF);
  static const lightTextInverse = Color(0xFFFFFFFF);

  static const lightNavBar = Color(0xFFFFFFFF);
  static const lightNavBarBorder = Color(0xFFE5E7EB);

  // ── Dark Mode ──────────────────────────────────────────
  static const darkBackground = Color(0xFF0F0F0F);
  static const darkSurface = Color(0xFF1A1A1A);
  static const darkSurfaceVariant = Color(0xFF252525);
  static const darkBorder = Color(0xFF2E2E2E);
  static const darkDivider = Color(0xFF2A2A2A);

  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFF9CA3AF);
  static const darkTextTertiary = Color(0xFF6B7280);
  static const darkTextInverse = Color(0xFF111827);

  static const darkNavBar = Color(0xFF1A1A1A);
  static const darkNavBarBorder = Color(0xFF2E2E2E);

  // ── Semantic ───────────────────────────────────────────
  static const success = Color(0xFF00C853);
  static const successLight = Color(0xFFE8F5E9);
  static const successDark = Color(0xFF1B5E20);

  static const warning = Color(0xFFF59E0B);
  static const warningLight = Color(0xFFFFF8E1);
  static const warningDark = Color(0xFF78350F);

  static const error = Color(0xFFEF4444);
  static const errorLight = Color(0xFFFEE2E2);
  static const errorDark = Color(0xFF7F1D1D);

  static const info = Color(0xFF3B82F6);
  static const infoLight = Color(0xFFEFF6FF);
  static const infoDark = Color(0xFF1E3A8A);

  // ── Status chips ───────────────────────────────────────
  static const statusFilledBg = Color(0xFFE8F5E9);
  static const statusFilledText = Color(0xFF2E7D32);
  static const statusMatchingBg = Color(0xFFFFF8E1);
  static const statusMatchingText = Color(0xFFF57F17);
  static const statusUpcomingBg = Color(0xFFEFF6FF);
  static const statusUpcomingText = Color(0xFF1D4ED8);
  static const statusPendingBg = Color(0xFFF3F4F6);
  static const statusPendingText = Color(0xFF374151);

  // ── Verified badge ─────────────────────────────────────
  static const verifiedBg = Color(0xFFE8F5E9);
  static const verifiedText = Color(0xFF2E7D32);
}
